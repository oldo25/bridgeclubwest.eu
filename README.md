# bridgeclubwest.eu

Static site for Bridge Club West, served by GitHub Pages from the `main` branch.

- `index.html` — the site
- `CNAME` — custom domain; GitHub Pages reads this. Do not delete.
- `.nojekyll` — skip Jekyll processing; serve files as-is.

Publishing: push to `main`. Pages rebuilds automatically.
